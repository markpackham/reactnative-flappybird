import React from "react";
import { View } from "react-native";

const Bird = () => {
  return (
    <View
      style={{
        position: "absolute",
        backgroundColor: "blue",
        width: "50px",
        height: "60px",
      }}
    />
  );
};

export default Bird;
